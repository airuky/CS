# 2. ポインタで値を読む・書き換える

## 目的

ポインタは「値そのもの」ではなく、値が置かれた**メモリ上の住所**を保持する変数だと理解する。

- `&x`：変数 `x` の住所を取得する
- `int *p`：`int` の住所を入れるためのポインタ変数を作る
- `*p`：`p` が持つ住所にある `int` の値を読む／書く

## 実験

次のプログラムを `2_pointer.c` として保存して実行する。

```c
#include <stdio.h>

int main(void) {
    int x = 10;
    int *p = &x;

    printf("x  = %d\n", x);
    printf("&x = %p\n", (void *)&x);
    printf("p  = %p\n", (void *)p);
    printf("*p = %d\n", *p);

    *p = 42;

    printf("*p = %d\n", *p);
    printf("x  = %d\n", x);
    return 0;
}
```

Macなら次でコンパイルできる。

```sh
clang -Wall -Wextra -Wpedantic -g 2_pointer.c -o 2_pointer
./2_pointer
```

## 観察すること

1. `&x` と `p` は、なぜ同じような住所を表示するか。
2. `*p` が最初に `10` を表示する理由は何か。
3. `*p = 42;` を実行しただけで、なぜ `x` も `42` になるのか。
4. `p` は `x` のコピーを持っているのか、それとも `x` の住所を持っているのか。

## 追加実験

次を `*p = 42;` の後に追加する。

```c
int y = 99;
p = &y;
*p = 100;

printf("x = %d\n", x);
printf("y = %d\n", y);
```

`p` の向き先を `x` から `y` に変えた後、どちらの値が変わるかを説明する。

## 注意

次のように、住所を入れずにポインタを使ってはいけない。

```c
int *p;
*p = 42;  // 未定義動作。実行しない。
```

`p` がどこを指すか決まっていないため、読み書き先も決まらない。ポインタを使う前には、必ず有効なオブジェクトの住所（例：`&x`）を代入する。

## 要点

> `p` は住所、`*p` はその住所にある実体。\
> `*p = 42` は、ポインタの先にあるメモリへ `42` を書き込む操作である。

## 振り返り

- `p` には `x` の住所 `&x` が格納されるため、`p` と `&x` は同じアドレスを表示する。
- `*p` は、`p` が指す先にある `int`、つまりこの場合は `x` そのものを表す。
- `*p = 42` は `x` の置かれたメモリに `42` を書き込むため、後から `x` を読むと `42` になる。

Javaのオブジェクト参照と似た面はあるが、Cのポインタはより低レイヤで、メモリ上の住所を型付きで扱うもの。CにはJavaやC++のような「参照渡し」という引数渡しはなく、関数の引数は常に値渡しである。ポインタを引数に渡すときも、渡されるのは住所のコピーである。
