# 3. 関数引数とポインタ：Cは常に値渡し

## 目的

Cの関数呼び出しでは、引数は常にコピーされて渡されることを理解する。

- `set_value(x)`：`x` の値をコピーして渡す
- `set_value_via_pointer(&x)`：`x` の住所をコピーして渡す

後者では、コピーされた住所を経由して元の `x` に書き込める。

## 実験

次を `3_function_pointer.c` として保存して実行する。

```c
#include <stdio.h>

void set_value(int value) {
    value = 42;
}

void set_value_via_pointer(int *address) {
    *address = 42;
}

int main(void) {
    int x = 10;

    set_value(x);
    printf("set_value の後: x = %d\n", x);

    set_value_via_pointer(&x);
    printf("set_value_via_pointer の後: x = %d\n", x);

    return 0;
}
```

```sh
clang -Wall -Wextra -Wpedantic -g 3_function_pointer.c -o 3_function_pointer
./3_function_pointer
```

## メモリのイメージ

```text
main                       set_value_via_pointer
----                       ---------------------
x = 10                     address = xの住所のコピー
                             |
                             +--> *address = 42 は x を書き換える
```

関数が受け取る `address` 自体はコピーだが、その中身は元の `x` を指す住所なので、`*address` を通じて `x` を変更できる。

## 答えること

1. `set_value(x)` に渡されるのは何のコピーか。
2. `set_value` の中で `value = 42;` としても、なぜ `main` の `x` は変わらないのか。
3. `set_value_via_pointer(&x)` に渡されるのは何のコピーか。
4. `address` はコピーなのに、なぜ `*address = 42;` は元の `x` を変えられるのか。

## 重要な結論

> Cの関数引数は常に値渡し。\
> ポインタを渡すと「住所のコピー」を渡すため、その住所の先にある元の値は変更できる。

将来、関数の中から呼び出し元のポインタそのものを差し替えたい場合は、ポインタへのポインタ（`int **`）を使う。これは後の課題で扱う。

## 振り返り

- `set_value(x)` には `x` の値（この例では `10`）のコピーが渡される。関数内の `value` は `x` とは別の変数なので、`value = 42` は `x` を変更しない。
- `set_value_via_pointer(&x)` には `x` の住所のコピーが渡される。
- コピーされたポインタでも、コピー元と同じ住所を保持している。したがって `*address = 42` は、元の `x` が置かれたメモリへ書き込む。
