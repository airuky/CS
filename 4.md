# 4. 関数呼び出しとスタックフレーム

## 目的

関数を呼び出すと、その関数用のローカル変数の置き場（**スタックフレーム**）が作られることを観察する。

前回の「値のコピー」と「住所のコピー」が、メモリ上ではどう違うのかを見る。

## 実験

次を `4_stack_frames.c` として保存して実行する。

```c
#include <stdio.h>

void observe(int value, int *address) {
    int local = 20;

    printf("[observe]\n");
    printf("value    = %d, &value    = %p\n", value, (void *)&value);
    printf("address  = %p, &address  = %p\n", (void *)address, (void *)&address);
    printf("*address = %d\n", *address);
    printf("local    = %d, &local    = %p\n", local, (void *)&local);
}

int main(void) {
    int x = 10;

    printf("[main]\n");
    printf("x = %d, &x = %p\n", x, (void *)&x);

    observe(x, &x);
    return 0;
}
```

```sh
clang -std=c17 -Wall -Wextra -Wpedantic -g 4_stack_frames.c -o build/4_stack_frames
./build/4_stack_frames
```

## 読み方

```text
main のスタックフレーム             observe のスタックフレーム
-----------------------             --------------------------
x = 10                              value = 10  （xの値のコピー）
                                    address = &x （xの住所のコピー）
                                    local = 20

address ───────────────────────────> x
```

`address` というポインタ変数そのものは `observe` の中にある。一方、その中身である `address` の値は `main` の `x` の住所である。

## 答えること

1. `x` と `value` は同じ `10` なのに、なぜ `&x` と `&value` は異なるか。
2. なぜ `address` の表示値は `&x` と同じになるか。
3. `address` と `&address` はそれぞれ何を表し、なぜ異なるか。
4. `x`、`value`、`address`、`local` はどの関数のスタックフレームに属するか。
5. プログラムを何度か実行したとき、絶対アドレスは変わり得るか。変わる理由は何か。

## 注意

変数の並び順や各アドレスの差は、C言語では保証されない。コンパイラ、最適化、CPU、OSの保護機構によって変わる。ここでは「関数ごとに別のローカル変数がある」ことと、「ポインタが別フレームの変数を指せる」ことを観察する。

## 要点

> 値渡しでは、呼び出された関数のスタックフレームに別の値が置かれる。\
> ポインタ渡しでは、呼び出された関数のスタックフレームに住所のコピーが置かれ、その先は呼び出し元の変数を指せる。
