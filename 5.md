# 5. ヒープと `malloc` / `free`

## 目的

関数の終了とともに自動で片付くローカル変数（通常はスタック）と、自分で確保・解放する動的メモリ（ヒープ）の違いを理解する。

## スタックとヒープの関係

スタックとヒープは親子関係ではない。同じプロセスが使うメモリの中にある、**用途と管理方法が異なる別々の置き場**である。

```text
1つのプログラム（プロセス）が見るメモリ

┌──────────────────────────────────────┐
│ コードやグローバル変数など             │
├──────────────────────────────────────┤
│ ヒープ: malloc が借りる領域            │
│   例: mallocした int の実体が置かれる  │
├──────────────────────────────────────┤
│ （ほかの領域）                         │
├──────────────────────────────────────┤
│ スタック: 関数のローカル変数など        │
│   例: stack_value や heap_value が置かれる │
└──────────────────────────────────────┘
```

図の上下や実際の位置は環境によって異なる。大切なのは、両者が別の置き場だということ。

```c
int stack_value = 10;
int *heap_value = malloc(sizeof *heap_value);
*heap_value = 42;
```

このコードでは次のようになる。

```text
スタック                              ヒープ
--------                              ------
stack_value = 10
heap_value = 0x5000  ───────────────> 0x5000: 42
```

`heap_value` はスタック上のローカル変数だが、その**中身**はヒープ上の場所の住所である。矢印を作っているのはポインタであり、ヒープがスタックの中にあるわけではない。

スタックは関数の呼び出し・終了に合わせて自動管理される。一方ヒープは、`malloc` で借りて `free` で返すまで残る。なお、ポインタ変数はスタック以外（ヒープやグローバル領域）に置くこともできる。

## まず `malloc` とは何か

`malloc` は「実行中のプログラムが、今必要な大きさの空きメモリを借りる」ための関数である。

```c
int *heap_value = malloc(sizeof *heap_value);
```

この1行は、次の順で読む。

1. `heap_value` は「`int` の住所を入れる変数」（`int *`）である。
2. `sizeof *heap_value` は「`heap_value` が指す先の `int` の大きさ」を表す。通常は4バイトだが、`4` と決め打ちしない。
3. `malloc(...)` がその大きさの空き領域を要求する。
4. 成功すれば、確保した領域の住所が返る。その住所が `heap_value` に入る。
5. `*heap_value = 42;` で、その住所の先にある領域へ `42` を書き込む。

`sizeof *heap_value` の `*` は実際にポインタをたどる操作ではない。`sizeof` の中では「指す先の型の大きさ」を調べているだけなので、まだ `heap_value` が初期化されていなくても安全である。

```text
malloc の直後

スタック（mainのローカル変数）            ヒープ（mallocが確保した領域）
--------------------------------            -----------------------------
heap_value = 0x5000  --------------------->  0x5000: 未初期化の int 1個分

*heap_value = 42 の後

heap_value = 0x5000  --------------------->  0x5000: 42
```

ここで `0x5000` は説明用の仮の住所であり、実際の値ではない。重要なのは、`heap_value` 自体は住所を持つローカル変数で、`42` が入る場所はその住所の先にある別の領域だという点。

Javaの `new` と少し似ているが、Cの `malloc` はオブジェクトを作るのではなく、**型のない生のバイト列**を返す。初期値は入っておらず、不要になったら自分で `free` する必要がある。

## 実験

次を `5_heap.c` として保存して実行する。

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int stack_value = 10;
    int *heap_value = malloc(sizeof *heap_value);

    if (heap_value == NULL) {
        fputs("malloc failed\n", stderr);
        return 1;
    }

    *heap_value = 42;

    printf("stack_value  = %d, &stack_value  = %p\n",
           stack_value, (void *)&stack_value);
    printf("heap_value   = %p, &heap_value   = %p\n",
           (void *)heap_value, (void *)&heap_value);
    printf("*heap_value  = %d\n", *heap_value);

    free(heap_value);
    heap_value = NULL;

    return 0;
}
```

```sh
clang -std=c17 -Wall -Wextra -Wpedantic -g 5_heap.c -o build/5_heap
./build/5_heap
```

## メモリのイメージ

```text
main のローカル領域（通常はスタック）       動的メモリ領域（ヒープ）
----------------------------------------       ------------------------
stack_value = 10                               *heap_value = 42
heap_value = ヒープの住所  ------------------->
```

`heap_value` はローカルなポインタ変数なので通常はスタックにある。一方、`malloc` が確保した `int` の置き場はヒープにある。

## 答えること

1. `stack_value`、`heap_value`、`*heap_value` はそれぞれどこにあるか。
2. `heap_value` と `&heap_value` はそれぞれ何を表すか。
3. `malloc(sizeof *heap_value)` の `sizeof *heap_value` は何バイト分を求めているか。
4. `malloc` の直後に `heap_value == NULL` を確認する理由は何か。
5. `free(heap_value)` の後に `*heap_value` を読んだり書いたりしてはいけない理由は何か。
6. `free` の後に `heap_value = NULL` とする利点は何か。

## 注意

`free(heap_value);` の後に `*heap_value` を読む／書くことは**未定義動作**である。試しに実行しない。`free` 後のポインタは、解放済みメモリを指すダングリングポインタになる。

また、Cでは `malloc` の戻り値をキャストしない。

```c
int *p = malloc(sizeof *p);        // よい
int *p = (int *)malloc(sizeof *p); // Cでは不要
```

## 要点

> スタック上のローカル変数は関数の終了時に自動で寿命を終える。\
> `malloc` したメモリは、不要になったら対応する `free` で解放する。\
> 解放後のポインタは使わず、必要なら `NULL` を代入して無効であることを明示する。

## 振り返り

`main` に書かれていることと、メモリ上のどこにあるかは分けて考える。

```text
main のローカル領域（通常はスタック）       動的メモリ領域（ヒープ）
----------------------------------------       ------------------------
stack_value = 10                               *heap_value = 42
heap_value = ヒープの住所  ------------------->
```

- `stack_value` と、ポインタ変数である `heap_value` 自体は、どちらも `main` のローカル変数で通常はスタックにある。
- `*heap_value` は、`malloc` が確保したヒープ上の `int` である。`main` がその住所を保持しているだけで、スタック上にはない。
- `heap_value` はヒープ領域の先頭の仮想アドレスを**値として保持する**。`&heap_value` は、そのポインタ変数自身がスタック上に置かれた住所である。
- `heap_value` の型は `int *` なので、`*heap_value` の型は `int`。したがって `sizeof *heap_value` は `int` 1個分であり、この環境では通常4バイトである。`sizeof` の中の `*` は実際にメモリを読まない。
- `malloc` が確保に失敗すると `NULL` を返す。`NULL` を参照することは無効なので、使う前に確認する。失敗時に任意の住所を返すわけではない。
- `free(heap_value)` の後、その領域はアロケータへ返され、別の用途に再利用され得る。ポインタの数値が残っていても、その領域を使う権利は失われるため、`*heap_value` の読み書きは未定義動作となる。これがuse-after-freeであり、状況によっては深刻な脆弱性になり得るが、RCEになるとは限らない。
- `free` の戻り値は `void` であり、成功・失敗を返さない。`heap_value = NULL` はこのポインタ変数を安全な値にして、うっかり再利用するのを防ぐために行う。ほかに同じ領域を指すポインタがあれば、それらは自動で `NULL` にならない。

## `malloc` の裏側：libcとカーネル

通常、プログラムの `malloc` が毎回カーネルへ直接頼みに行くわけではない。

```text
プログラムの malloc(必要なバイト数)
        ↓
libc のメモリアロケータ
        ↓ 空き領域があれば切り分けて住所を返す
        ↓ 空き領域が足りなければ
カーネルに大きめの領域を要求
```

`free` もまずは libc のアロケータへ領域を返す操作である。アロケータは次の `malloc` に再利用することが多く、毎回ただちにカーネルへ返すわけではない。

## 仮想アドレスとの関係

現代のOS上では、ポインタが保持する住所や `malloc` が返す住所は通常、プロセスから見た**仮想アドレス**である。

```text
Cのコード                    OS・CPUの内部

&x で住所を得る         →    仮想アドレス
*p で住所の先を読む      →    MMUが物理メモリへ対応付ける
```

`&` と `*` はC言語の演算子であり、仮想アドレスはOSとCPUが実現する仕組みである。今は「ポインタの住所は通常、仮想アドレス」と理解すれば十分。

## `NULL` と未初期化ポインタ

`malloc` が確保に失敗した場合は `NULL` を返す。`NULL` は「有効な場所を指していない」と明示する、定義された特別なポインタ値である。

```c
int *p = NULL;  // 有効。何も指していない状態

if (p == NULL) {
    // pは参照できない
}
```

ただし、`NULL` を参照してはいけない。

```c
*p = 42;  // 未定義動作
```

未初期化ポインタとは区別する。

```c
int *a;        // 中身が不定。どこを指すか不明
int *b = NULL; // 何も指していない、と明示された状態
```

## 同じ名前の別概念：データ構造のヒープ

「ヒープ」には、優先度付きキューなどに使う木構造（データ構造のヒープ）という別の意味もある。これは `malloc` が使うヒープ領域とは直接関係がない。

```text
データ構造のヒープ  = 親子の大小関係を保つ木・配列の構造
メモリのヒープ      = malloc/freeで管理する動的メモリ領域
```

ただし、データ構造のヒープを `malloc` したメモリ領域上に実装することはできる。

## 第5章の要点

1. `heap_value` はスタック上のポインタ変数、`*heap_value` はヒープ上の実体。
2. `malloc(sizeof *heap_value)` は `int` 1個分（通常4バイト）の領域を借り、その先頭住所を返す。
3. `*heap_value = 42` は、ポインタが指すヒープ上の領域へ値を書き込む。
4. `malloc` の失敗時は `NULL` が返るため、参照する前に確認する。
5. `free` 後の領域は使えない。ポインタを `NULL` にして、誤った再利用を防ぐ。
6. スタックは関数の開始・終了に合わせて自動管理され、ヒープは `malloc` / `free` で明示的に管理される。
