# 7. 動的配列を関数に渡す

## 目的

ヒープ上の配列を関数に渡すとき、関数が受け取るのは先頭アドレスのコピーであり、配列の長さは別途渡す必要があることを理解する。

## 実験

次を `7_functions_and_arrays.c` として保存して実行する。

```c
#include <stdio.h>
#include <stdlib.h>

void print_numbers(const int *values, size_t count) {
    for (size_t i = 0; i < count; i++) {
        printf("values[%zu] = %d\n", i, values[i]);
    }
}

void increment_all(int *values, size_t count) {
    for (size_t i = 0; i < count; i++) {
        values[i] += 1;
    }
}

int main(void) {
    size_t count = 4;
    int *numbers = malloc(count * sizeof *numbers);

    if (numbers == NULL) {
        fputs("malloc failed\n", stderr);
        return 1;
    }

    for (size_t i = 0; i < count; i++) {
        numbers[i] = (int)(i * 10);
    }

    puts("before increment:");
    print_numbers(numbers, count);

    increment_all(numbers, count);

    puts("after increment:");
    print_numbers(numbers, count);

    free(numbers);
    numbers = NULL;
    return 0;
}
```

```sh
clang -std=c17 -Wall -Wextra -Wpedantic -g 7_functions_and_arrays.c -o build/7_functions_and_arrays
./build/7_functions_and_arrays
```

## メモリのイメージ

`increment_all(numbers, count)` を呼ぶときの状態は次の通り。

```text
main のスタック                         increment_all のスタック
---------------                         ----------------------
numbers = 0x5000  --------------------> values = 0x5000
count = 4                               count = 4
                                               |
                                               +--> values[i] はヒープ上の配列を変更する

ヒープ
-----
0x5000: numbers[0]
0x5004: numbers[1]
0x5008: numbers[2]
0x500c: numbers[3]
```

`values` は `numbers` の住所のコピーだが、同じヒープ領域を指している。そのため `values[i] += 1` は、呼び出し元の `numbers[i]` を変更する。

## なぜ `count` も渡すのか

`int *values` には先頭アドレスしか入っていない。どこまでが配列なのかという長さの情報は、ポインタだけからは分からない。

```c
void print_numbers(const int *values, size_t count)
```

そのため、動的配列を関数に渡すときは、通常「先頭ポインタ + 要素数」をセットで渡す。

## `const int *` の意味

```c
void print_numbers(const int *values, size_t count)
```

`const int *values` は「この関数は `values` を通じて配列の要素を書き換えない」という宣言である。

```c
values[0] = 99;  // print_numbers 内ではコンパイルエラー
```

一方、`increment_all` は配列を変更するため `int *values` としている。

## 配列引数の書き方

関数の引数では、次の2つは同じ意味になる。

```c
void f(int *values, size_t count);
void f(int values[], size_t count);
```

どちらも関数にはポインタが渡る。配列の長さは自動では渡らない。

## 答えること

1. `increment_all(numbers, count)` で、`values` と `count` にはそれぞれ何のコピーが渡るか。
2. `values` はコピーなのに、なぜ `values[i] += 1` で `numbers[i]` が変わるか。
3. `int *values` だけでは、なぜ配列の長さが分からないか。
4. `print_numbers` の引数を `const int *values` にする利点は何か。
5. `free(numbers)` を `increment_all` や `print_numbers` の中で呼ばず、`main` で呼ぶ理由は何か。
6. `increment_all(numbers, count + 1)` を呼ぶと何が危険か。

## 要点

> 動的配列は「先頭ポインタ + 要素数」をセットで関数へ渡す。\
> ポインタはコピーされても、同じヒープ領域を指すため、その先の要素は変更できる。\
> 確保した側が、不要になった時点で1回だけ `free` する。
