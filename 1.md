# 1. Cの配列 `buf` と `&buf`

## 目的

配列はポインタではないこと、ただし通常の式では先頭要素へのポインタに変換されることを理解する。

## 実験

`1.c` をビルドして実行する。

```sh
clang -std=c17 -Wall -Wextra -Wpedantic -g 1.c -o build/1
./build/1
```

次の4点を観察する。

1. `buf` と `&buf` は、なぜ同じような住所を表示するか。
2. `buf + 1` と `&buf + 1` は、それぞれ何バイト先を示すか。
3. `x` と `buf` の絶対アドレスや並び順は、毎回・すべての環境で同じといえるか。
4. `0x41414141` はASCII文字列として何を表すか。

## 配列の基本

```c
char buf[8];
```

この宣言でできる `buf` は、8個の `char` が連続した**配列**である。`buf` 自体はポインタではない。

## `buf` と `&buf` の違い

通常の式で `buf` を使うと、Cの規則により先頭要素のアドレス `&buf[0]` に変換される。

```c
buf      // 通常は &buf[0] に変換される。型は char *
&buf     // 配列全体のアドレス。型は char (*)[8]
```

`buf` と `&buf` はどちらも配列が始まる場所を示すため、表示される数値のアドレスは同じに見える。

```text
アドレス     中身
0x1000       buf[0]  ← buf と &buf の開始位置
0x1001       buf[1]
...
0x1007       buf[7]
```

ただし、何を指すポインタか（型）が違う。

| 式 | 型 | 指すもの |
| --- | --- | --- |
| `buf` | `char *` | 先頭の要素 `buf[0]` |
| `&buf` | `char (*)[8]` | `char` が8個ある配列全体 |

## `+ 1` で違いが分かる

ポインタ演算は、指している型の大きさだけ進む。

```c
buf + 1    // 次の char へ進む（1バイト先）
&buf + 1   // 次の char[8] 配列へ進む（8バイト先）
```

たとえば `buf` の先頭が `0x1000` の場合、次のようになる。

```text
buf       = 0x1000
&buf      = 0x1000
buf + 1   = 0x1001
&buf + 1  = 0x1008
```

`&` はサイズを取る演算子ではなく、対象のアドレスを取る演算子である。`&buf + 1` が8バイト先へ進むのは、`&buf` が「8要素の配列」を指す型だから。

## なぜ `buf` は `&buf[0]` に変換されるのか

Cでは配列を関数に効率よく渡し、その各要素を扱えるよう、通常の式では配列を先頭要素へのポインタとして扱う。

```c
void show(char *p);

char buf[8];
show(buf);  // 先頭要素のアドレスが渡る
```

配列全体をコピーせず、先頭のアドレスを渡せば、関数の中で `p[0]`、`p[1]` のように配列の要素を参照できる。

## 配列のまま扱われる場面

`sizeof` や `&` の対象では、`buf` は `&buf[0]` に変換されず、配列のまま扱われる。

```c
sizeof(buf)   // 8: 配列全体のサイズ
sizeof(&buf)  // ポインタのサイズ（64ビット環境では通常8）
```

## 要点

> `buf` は配列そのもの。通常の式では `&buf[0]` に変換される。\
> `&buf` は配列全体へのポインタであり、数値のアドレスは同じでも型と `+ 1` の進み方が違う。

## 振り返り

- `buf` は通常の式では `&buf[0]` に変換されるため、`buf` と `&buf` はどちらも配列の先頭位置を示し、数値のアドレスは同じに見える。
- `buf` は `char *` として扱われるので `buf + 1` は1バイト先へ進む。`&buf` は `char (*)[8]` なので `&buf + 1` は配列全体の8バイト先へ進む。
- ローカル変数の絶対アドレスはASLRなどにより実行ごとに変わり得る。また、変数の並び順・間隔はC言語では保証されず、コンパイラや最適化によっても変わる。
- `0x41` はASCIIの `A`。`0x41414141` は4バイトすべてが `0x41` なので `AAAA` を表す。
