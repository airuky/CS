# 6. ヒープ上の動的配列

## 目的

実行時に必要な個数を決められる `int` 配列を `malloc` で作り、配列アクセスとポインタ演算が同じ仕組みであることを理解する。

## 実験

次を `6_dynamic_array.c` として保存して実行する。

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    size_t count = 4;
    int *numbers = malloc(count * sizeof *numbers);

    if (numbers == NULL) {
        fputs("malloc failed\n", stderr);
        return 1;
    }

    for (size_t i = 0; i < count; i++) {
        numbers[i] = (int)((i + 1) * 10);
    }

    for (size_t i = 0; i < count; i++) {
        printf("numbers[%zu] = %d, address = %p\n",
               i, numbers[i], (void *)&numbers[i]);
    }

    free(numbers);
    numbers = NULL;
    return 0;
}
```

```sh
clang -std=c17 -Wall -Wextra -Wpedantic -g 6_dynamic_array.c -o build/6_dynamic_array
./build/6_dynamic_array
```

## メモリのイメージ

```text
スタック                                      ヒープ
--------                                      ------
count = 4
numbers = 0x5000  --------------------------->  0x5000: numbers[0] = 10
                                                0x5004: numbers[1] = 20
                                                0x5008: numbers[2] = 30
                                                0x500c: numbers[3] = 40
```

`numbers` はヒープ領域の先頭を指すポインタであり、`numbers[0]` から `numbers[3]` は連続した4個の `int` である。実際のアドレスは環境によって変わるが、隣同士の `int` は通常4バイトずつ離れている。

## 配列アクセスとポインタ演算

次の2つは同じ意味である。

```c
numbers[i]
*(numbers + i)
```

たとえば `numbers + 1` は、次の `int` へ進む。`numbers` の型が `int *` なので、通常4バイト先を指す。

## 答えること

1. `numbers` 自体と、`numbers[0]` から `numbers[3]` はそれぞれどこにあるか。
2. `malloc(count * sizeof *numbers)` は、この例では何バイトを要求するか。
3. `numbers + 1` は何バイト先を指すか。理由も説明する。
4. `numbers[2]` をポインタ演算で書くとどうなるか。
5. なぜ `free(numbers)` は1回だけでよく、`numbers[0]` から `numbers[3]` に個別の `free` が不要なのか。
6. `numbers[4]` へ書き込んではいけない理由は何か。

## 注意

このコードが確保したのは `numbers[0]` から `numbers[3]` までの4要素だけである。

```c
numbers[4] = 50;  // 範囲外アクセス。未定義動作。実行しない。
```

配列の範囲外アクセスは、後に学ぶメモリ破壊の出発点になる。まずは「確保した個数 `count` の範囲だけを使う」習慣を固める。

## 要点

> `malloc(count * sizeof *numbers)` は、`int` を `count` 個連続して置けるヒープ領域を確保する。\
> `numbers[i]` は `*(numbers + i)` と同じ。\
> 確保した配列全体は、先頭ポインタ `numbers` を1回 `free` して返す。

## 振り返り

- ポインタ変数 `numbers` 自体は `main` のローカル変数なので通常はスタックにある。その中身はヒープ領域の先頭住所。`numbers[0]` から `numbers[3]` の実体はヒープ上に連続してある。
- この例では `count` が4、`sizeof *numbers` は `int` 1個分の通常4バイトなので、`malloc` は `4 × 4 = 16` バイトを要求する。
- `numbers` の型は `int *`。したがって `numbers + 1` は「次の `int`」である `&numbers[1]` を指し、通常**4バイト先**へ進む。配列全体の16バイト先ではない。

```text
numbers     = &numbers[0]
numbers + 1 = &numbers[1]  （通常4バイト先）
numbers + 2 = &numbers[2]  （通常8バイト先）
```

- `numbers[2]` は `*(numbers + 2)` と同じ。`(*numbers) + 2` ではない。後者は「先頭要素の値」に2を足す式であり、このプログラムなら `10 + 2`、つまり `12` になる。
- 1回の `malloc` が4要素をまとめた1つの連続領域を確保しているため、返すときも `free(numbers)` を1回呼ぶ。`free(numbers + 1)` のように途中の要素の住所を渡してはいけない。
- `numbers[4]` は確保範囲 `numbers[0]` から `numbers[3]` の外なので、書き込むと未定義動作になる。

## 固定長配列との違い

第1章の `char buf[8]` は配列そのものだった。一方、ここでの `numbers` は `malloc` が返した住所を保持する**ポインタ変数**であり、型は `int *` である。

```c
char buf[8];                         // 配列。buf + 1 は1バイト先
int *numbers = malloc(4 * sizeof *numbers); // ポインタ。numbers + 1 は次のintへ
```

どちらも「次の要素」へ進むが、進む幅は要素型の大きさで決まる。`int *` の `numbers + 1` は配列全体を飛び越える操作ではない。
